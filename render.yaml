services:
  - type: web
    name: fitfind-backend
    runtime: python
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --timeout 120
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0